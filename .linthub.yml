platform: linux

build:
 - mkdir build
 - cd build
 - cmake ..
 - make

test:
 - make test

analyzers:
 - cpplint: true
   include: ["*.cpp", "*.h", "*.hpp", "*.c"]
   path: ./src/
   filters:
   - -whitespace
   - +whitespace/braces
   style: google # webkit | google

 - polysquare-cmake: true
   allowed: ["CMakeLists.txt"]

 - markdownlint: true
